description     "Run collectd via docker"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn

script
    /usr/bin/docker run -i -p {{collectd_port}}:25826/udp -p {{collectd_web_port}}:8080 {{collectd_container}}
end script
